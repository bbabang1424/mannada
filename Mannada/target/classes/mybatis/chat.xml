<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chat">
	<select id="manna_list" resultType="Manna">
		SELECT m.id, category, title, d_day
		FROM partner p LEFT OUTER JOIN manna m ON p.manna_id = m.id
		WHERE p.num = #{num}
	</select>
	
	<select id="chat_list" resultType="Chat">
		SELECT c.*
		FROM chatmessage c LEFT OUTER JOIN user u ON c.num = u.num
		WHERE c.manna_id = #{mannaId}
	</select>
</mapper>