<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="login" resultType="User">
		SELECT * FROM user
		WHERE id=#{id} AND pw=PASSWORD(#{pw})
	</select>

	<insert id="signup">
		INSERT INTO user(id, pw, name, nickname, reg_date)
		VALUES (#{id}, PASSWORD(#{pw}), #{name}, #{nickname}, NOW())
	</insert>	
	
	<select id="checkId" resultType="Integer">
		SELECT COUNT(*) FROM user
		WHERE id=#{id}
	</select>	
	
	<insert id="add_partner">
		INSERT INTO joins
		VALUES (#{mannaId}, #{num})
	</insert>
	
	<!-- 회원정보수정 및 탈퇴 관련 -->
	<select id="profile" resultType="User">
		SELECT * FROM user
	</select>
	
	<select id="item" resultType="User">
		SELECT * FROM user
		WHERE id=#{id}
	</select>
	
	<update id="proUpdate">
		UPDATE user
		SET name=#{name}, nickname=#{nickname}, phone=#{phone}, img_url=#{imgUrl}
		WHERE id=#{id}
	</update>
	
	<select id="checkPW" resultType="Integer">
		SELECT COUNT(*) FROM user 
		WHERE id=#{id} AND pw=#{pw};
	</select>
	
	<update id="updatePW">
		UPDATE user
		SET pw=#{newPW} 
		WHERE id=#{id} AND pw=#{pw}
	</update>
</mapper>